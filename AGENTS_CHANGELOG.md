2026-03-25 codex init: read CLAUDE.md + repo, recorded status. why: sync with Claude, cut repeated context.
2026-03-25 project: Vite/React/MUI/ECharts MVP exists; Fuller/A&A/MAA + moisture + validation + feedback wired. why: current baseline.
2026-03-25 risk: build passes, but no tests/backend/persistence; bundle large; some logic still heuristic. why: delivery risk snapshot.
2026-03-25 req split: customer docs mapped to modules + standalone inputs. why: align scope with Claude/user.
2026-03-25 ui split: App rebuilt as independent module workbench (Fuller/A&A/MAA/Safety/Moisture/Feedback). why: support partial data + progressive use, keep formulas unchanged.
2026-03-25 ui rework: removed module cards/tabs; now left workflow inputs + right fixed results/chart, with upstream autofill, custom override toggles, required checks. why: match desktop ops flow.
